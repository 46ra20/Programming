#include <bits/stdc++.h>
using namespace std;
int main()
{
    // vector<int> v;
    // // v;
    // v.push_back(10);
    // v.push_back(10);
    // v.push_back(10);
    // v.push_back(10);
    // cout << v.size() << endl;
    // for (int val : v)
    // {
    //     cout << val << " ";
    // }

    vector<int> v2 = {1, 3, 5, 7, 8};
    vector<int> v(v2);
    for (int val : v)
        cout << val << " ";

    return 0;
}